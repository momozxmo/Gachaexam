# 📝 Short Reflection — Gacha Drop Rate Simulator

สรุปการเรียนรู้จากการทำโปรเจกต์เว็บจำลองกาชาด้วย AI (Claude Code / Opus 4.8)

---

## ทำจริงเป็นอย่างไร

โดยรวมทำงานแบบ **"ให้สเปกครบก่อน แล้วค่อยให้ AI ลงมือ"** ผมเตรียม requirement ทั้งหมดเป็นตาราง 3 ระดับ (Must / Advanced / Bonus) พร้อมค่าเริ่มต้น (SSR 1%, SR 9%, R 30%, N 60%), ผัง UI และข้อจำกัดเทคนิค (ไฟล์เดียว, vanilla JS, CSV ผ่าน Blob, ห้าม hardcode) ส่งให้ AI ในครั้งเดียว ทำให้ AI สร้างทั้งแอปได้ตรงสเปกตั้งแต่รอบแรก ไม่ต้องวนแก้หลายรอบ

หลังจากได้ไฟล์แล้ว ผมใช้ **เกณฑ์การให้คะแนนเป็นตัวขับการปรับปรุง (rubric-driven)** — ส่ง rubric หลักและโบนัสให้ AI ตรวจงานตัวเองทีละข้อ แล้วค่อยสั่งยกระดับเฉพาะจุดที่ยังไม่เต็ม จังหวะการทำงานเลยเป็นวงรอบสั้น ๆ: สเปก → สร้าง → ตรวจด้วยเกณฑ์ → ขัดเงา

---

## จุดที่ทำได้ดี

- **สเปกละเอียด = ผลลัพธ์ตรง** ยิ่ง requirement ชัด (ใส่ค่าเริ่มต้น/พฤติกรรมที่คาดหวัง/ข้อห้าม) AI ยิ่งเดาน้อยและงานตรงเป้า
- **AI พิสูจน์ logic ด้วยการรันจริง** ไม่ได้เชื่อว่าโค้ดถูกเฉย ๆ แต่รัน Node.js ทดสอบ: การกระจาย rate ลู่เข้า 1/9/30/60 จริง, Pity การันตี SSR เป๊ะทุก X ครั้ง, สูตร free roll (฿3000÷฿30 → 100 paid + 10 free = 110) และ Monte Carlo ออกมา ~67% ตรงกับตัวอย่างในสเปก
- **คุมขอบเขตได้** เพราะตัดสินใจเรื่อง "ไฟล์เดียว/ไม่มี build" ไว้ตั้งแต่ต้น ทำให้ไม่หลุดไปใช้ไลบรารีหรือ framework เกินจำเป็น

---

## ติดปัญหาตรงไหน

- **เครื่องมือบน Windows งอแง** บางคำสั่ง (`wc`, PowerShell บางจังหวะ) คืนค่าว่าง/ error ต้องสลับไปใช้เครื่องมืออื่นเพื่อยืนยันสถานะไฟล์
- **เรื่อง performance ของ Monte Carlo** ต้องระวังว่าถ้า simulation × rolls ต่อรอบมากเกินไปจะหน่วง เลยออกแบบให้ตอนรัน Monte Carlo นับเฉพาะ SSR (ไม่สุ่ม item) เพื่อให้เร็วพอกับ 100,000 รอบ
- **UX ของการแก้ไข item รอบแรกยังดิบ** ใช้กล่อง `prompt()` ของเบราว์เซอร์ ซึ่งทำงานได้แต่ดูไม่โปร

---

## ตอนที่ AI แนะนำ/ทำผิด แล้วแก้ยังไง


1. **AI เลือกวิธีแก้ไข item ด้วย `prompt()` ซึ่งไม่ดีพอ**
   เป็นทางออกที่ "ทำงานได้แต่ไม่เนียน"
   **แก้โดย:** ใช้เกณฑ์ Polish เป็นตัวจี้ แล้วสั่งให้เปลี่ยนเป็น **inline edit ในตาราง** (แก้ในแถวได้เลย + Enter/Esc + กันชื่อซ้ำ) บทเรียนคือ **AI มักหยุดที่ "พอใช้" ถ้าเราไม่ตั้งมาตรฐานให้สูงกว่านั้น**

2. **ความเสี่ยง CSV ภาษาไทยเพี้ยน / คอลัมน์เพี้ยน**
   ค่าที่มี comma หรือเครื่องหมายคำพูดอาจทำให้ไฟล์ CSV คอลัมน์เลื่อน และภาษาไทยอ่านใน Excel ไม่ออก
   **แก้โดย:** เพิ่ม **UTF-8 BOM (`﻿`)** และ **escape เซลล์ที่มีอักขระพิเศษ** ก่อน export

---

## สิ่งที่ได้เรียนรู้

> AI เก่งที่สุดเมื่อเรา (1) ให้สเปกชัดตั้งแต่ต้น (2) ใช้เกณฑ์วัดผลเป็นตัวกำกับคุณภาพ และ (3) บังคับให้ "พิสูจน์ว่าถูก" ด้วยการรันทดสอบจริง ไม่ใช่แค่เชื่อคำตอบ — หน้าที่ของคนคือ **ตั้งทิศทาง ตั้งมาตรฐาน และตรวจสอบ** ส่วน AI รับหน้าที่ลงรายละเอียดและทำงานซ้ำ ๆ ให้เร็ว
